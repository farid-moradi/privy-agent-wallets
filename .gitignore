# secrets — never commit
.env

# created by `setup`, specific to your Privy app
wallets*.json

# build output (root-anchored so cmd/ entries stay tracked)
/agent-wallets
/privy-agent-wallets
/user-wallet-demo
